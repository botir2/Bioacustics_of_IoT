package com.example.acousticapp.map

import android.os.Bundle
import androidx.appcompat.app.AppCompatActivity
import com.example.acousticapp.R

class DeviceMapActivity : AppCompatActivity() {
    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        setContentView(R.layout.activity_device_map)
        
        // Placeholder for future Google Maps initialization
        // Note: Remember to add API Key in AndroidManifest.xml when ready.
    }
}
